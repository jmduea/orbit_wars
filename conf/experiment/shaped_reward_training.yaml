defaults:
  - full_training

env:
  reward_capture_planet: 0.1
  reward_ship_delta: 0.001
  reward_production_delta: 0.02
run_name: orbit_wars_ppo_shaped_reward
save_dir: artifacts/shaped_reward_training
