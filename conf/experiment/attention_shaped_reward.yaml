# @package _global_

defaults:
  - attention_training
  - override /curriculum: self_play_staged
  - override /opponent_mix: self_play_curriculum

env:
  reward_capture_planet: 0.1
  reward_ship_delta: 0.001
  reward_production_delta: 0.02
run_name: orbit_wars_ppo_attention_shaped_reward
save_dir: artifacts/attention_shaped_reward_training
