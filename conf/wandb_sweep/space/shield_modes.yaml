# @package _global_

parameters:
  task:
    values:
      - shield_off
      - shield_cheap
      - shield_tiered

  task.feature_history_steps:
    values: [5, 10, 15, 20]

  telemetry.wandb.tags:
    value: [shield_modes_history_sweep]
