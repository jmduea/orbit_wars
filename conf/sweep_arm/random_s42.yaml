# @package _global_
# Post-encoder-once overnight arm: held-out random learnability, seed 42.

defaults:
  - override /opponents: random_only
  - override /curriculum: "off"
  - _self_

seed: 42
output:
  campaign: post_encoder_overnight_random_s42
training:
  total_updates: 500
