# @package _global_
# Post-encoder-once overnight arm: stable self-play, seed 42.

defaults:
  - override /opponents: self_play_only
  - override /curriculum: "off"
  - _self_

seed: 42
output:
  campaign: post_encoder_overnight_stable_s42
training:
  total_updates: 5000
