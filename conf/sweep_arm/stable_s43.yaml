# @package _global_
# Post-encoder-once overnight arm: stable self-play, seed 43.

defaults:
  - override /opponents: self_play_only
  - override /curriculum: "off"
  - _self_

seed: 43
output:
  campaign: post_encoder_overnight_stable_s43
training:
  total_updates: 5000
