defaults:
  - base
  - _self_
# mostly meant for validation/debugging
trajectory_shield_enabled: true
trajectory_shield_mode: exact
trajectory_shield_final_validate_selected: false
trajectory_shield_horizon: 500