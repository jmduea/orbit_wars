defaults:
  - base
  - _self_

checkpoint_every: 0

artifact_pipeline:
  enabled: false
  replay_async: false
  docker_validation_async: false
  worker_autostart: false
  ledger_enabled: false

replay:
  enabled: false
